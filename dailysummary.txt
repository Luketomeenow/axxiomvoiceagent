AXXIOM VOICE AGENTS — DAILY SUMMARY
Date: 2026-07-01 (covers the last ~24 hours)

=====================================================================

=====================================================================
Wired real Twilio phone numbers into Vapi so each brand can dial from its
own local caller ID WITHOUT Vapi's daily free-number call cap, and shipped
a live multi-campaign monitor on the dashboard that shows every running
campaign's dial / active / qualified counts in realtime.

=====================================================================
1. TWILIO NUMBERS -> VAPI (escape the daily free-number cap)
=====================================================================
- Added a one-command importer that registers your Twilio DIDs in Vapi and
  returns each number's Vapi phoneNumberId (the id the dialer already uses
  per brand):
    bun run import-twilio-numbers --brand quality --number +13017999116
    bun run import-twilio-numbers data/twilio-numbers.csv   (batch: brand,number[,name])
    bun run import-twilio-numbers --list                    (show what's already in Vapi)
- WHY: Vapi's own free/bought numbers have a DAILY outbound-call limit --
  the exact error the dialer's isSystemicDialError() guard already
  auto-pauses a campaign on. Imported Twilio numbers remove that cap and
  give each brand a real local caller ID.
- New files / config:
    scripts/import-twilio-numbers.ts  (idempotent: re-runs match by E.164,
                                       so a number is never imported twice)
    src/config/env.ts: TWILIO_ACCOUNT_SID + TWILIO_AUTH_TOKEN + assertTwilio()
    package.json: "import-twilio-numbers" (+ ":node" fallback)
- Confirmed the exact Vapi import API (POST /phone-number, provider "twilio")
  against Vapi's live OpenAPI spec. Typecheck passes (bun run typecheck).

  TO FINISH (manual, ~10 min):
    a) Put TWILIO_ACCOUNT_SID + TWILIO_AUTH_TOKEN in .env
       (Twilio Console -> Account -> API keys & tokens).
    b) Run the importer for each brand's number, then paste the returned
       phoneNumberId into that brand's vapiPhoneNumberId in
       src/assistant/brands.ts (the 6 UUIDs there today are Vapi numbers --
       `--list` shows provider, so any already [twilio] are done).
    c) Register STIR/SHAKEN + CNAM on the Twilio numbers so outbound caller
       ID isn't flagged "Spam Likely" (voice reputation, not A2P/SMS).

=====================================================================
2. LIVE CAMPAIGN MONITOR (dashboard)
=====================================================================
- New web/components/LiveCampaigns.tsx on the console page: a realtime
  monitor of EVERY running campaign at once (not just the selected one).
- Per campaign it shows calls dialed this run, active / in-flight calls,
  and qualified count; updates via Supabase Realtime (campaign + call
  changes) plus a 5s poll, and appears the moment a campaign starts.
- Click a campaign to select it. Complements the multi-campaign dialing
  worker (each running campaign dials on its own concurrency budget).

=====================================================================
STILL PENDING / NEXT
=====================================================================
- Twilio: add creds -> import numbers -> paste ids into brands.ts ->
  STIR/SHAKEN + CNAM (see section 1). Nothing else in the dialer changes.
- Optional: DB-backed per-brand number override (brand_phone:<slug> in
  outbound.app_setting, mirroring brand_assistant:<slug>) so caller-ID
  numbers can be swapped without editing brands.ts and redeploying.
